version: '3.8'

services:
  sahayak-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sahayak-mission-control
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - ENVIRONMENT=production
    restart: unless-stopped
